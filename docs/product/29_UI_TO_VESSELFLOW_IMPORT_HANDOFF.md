# UI to VesselFlow Import Handoff

The UI now shows VesselFlow waiting for user data.

Required:

- dataset/workbook;
- expected nomination flow;
- qualification rules;
- known blocking rules.

Next bundle should create a controlled import runner.
