# Real Data Delta Review

## Purpose

Compare the current VesselFlow controlled state with the real/anonymized intake preview.

The output shows whether the product is ready for a controlled `--write --rerun-state`.

Current expected result before real data:

```text
ready_for_write_rerun: false
risk_change_expected: NO_REDUCTION_YET_WAITING_FOR_DATA
```
