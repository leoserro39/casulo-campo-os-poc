# Real Data Review Protocol

Before using real/anonymized data:

1. classify the data;
2. validate intake with `--check`;
3. review warnings/errors;
4. only then run `--write --rerun-state`;
5. regenerate report export and delta review;
6. keep all external/production actions blocked.
