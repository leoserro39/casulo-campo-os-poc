# State Rerun Protocol

The rerun updates:

- VesselFlow import input;
- controlled state definition outputs;
- report export can be regenerated next.

Sample placeholder mode should only change after explicit `--write`.
