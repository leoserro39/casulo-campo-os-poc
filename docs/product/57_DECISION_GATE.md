# Decision Gate

The decision gate is conservative by default.

It never authorizes:

- automatic nomination;
- client-facing claim;
- implementation execution;
- production activation.

It can only approve controlled internal review/demo states.
