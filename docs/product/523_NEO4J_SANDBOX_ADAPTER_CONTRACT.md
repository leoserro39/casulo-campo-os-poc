# 523 Neo4J Sandbox Adapter Contract

The adapter is sandbox-only. It cannot connect to production Neo4j. It cannot handle production credentials. It cannot write to a live graph unless an explicit sandbox import flag is set in a later phase.
