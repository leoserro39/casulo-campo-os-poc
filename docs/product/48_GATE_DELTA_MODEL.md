# Gate Delta Model

Gate delta is not allowed to unlock execution by itself.

The model can only state that gates may move from `BLOCKED_SAMPLE_DATA` to `CHECK_REQUIRED` after a controlled data-backed rerun.

Human review remains required.
