{
  "contract": "casulo.common_workload_calibration_stress_lab.v0.1",
  "purpose": "Run a stronger calibration stress lab for routine workloads before enabling real-case agent usage.",
  "default_batch": "1200 cases, 100 per workload",
  "blocked_actions": [
    "client_facing_claim",
    "automatic_nomination",
    "implementation_execution",
    "production_activation",
    "automatic_merge",
    "credential_handling"
  ]
}
