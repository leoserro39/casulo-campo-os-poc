# VesselFlow Import Templates

Use `vesselflow_import_input_template.json` as the base structure when real/anonymized VesselFlow data is ready.

For now, the sample input keeps the system in controlled local demo mode.
